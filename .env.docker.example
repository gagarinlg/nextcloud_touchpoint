# Copy this file to .env.docker (gitignored) and fill in real values before
# running `docker compose up`. Never commit .env.docker.
POSTGRES_PASSWORD=changeme
NEXTCLOUD_ADMIN_USER=admin
NEXTCLOUD_ADMIN_PASSWORD=changeme
