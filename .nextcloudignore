# SPDX-FileCopyrightText: 2026 Touchpoint Contributors
# SPDX-License-Identifier: AGPL-3.0-or-later
#
# Files/dirs excluded from the packaged / deployed app (rsync --exclude-from).
# Everything NOT listed here ships: appinfo/ lib/ js/ css/ img/ templates/
# l10n/ LICENSES/ REUSE.toml.

# VCS & project meta
/.git
/.github
/.gitignore
/.gitattributes
/.nextcloudignore
/.claude
/CLAUDE.md
/README.md
/CHANGELOG.md
/CONTRIBUTING.md
/AUTHORS.md
/docs
/screenshots
/Makefile

# Frontend sources & toolchain (only the built js/css ship)
/src
/node_modules
/vite.config.mjs
/.eslintrc.cjs
/package.json
/package-lock.json

# PHP dev toolchain (no runtime composer deps)
/vendor
/composer.json
/composer.lock
/phpstan.neon
/phpunit.xml

# Tests & their artifacts
/tests
/e2e
/playwright.config.js
/.phpunit.cache
/test-results

# Dev scripts & migration helper (not part of the running app)
/scripts
/import_egroupware.py
/__pycache__

# Build staging & source maps (not needed at runtime)
/build
*.map
*.map.license

# Local dev-environment scaffolding (not part of the running app; see
# .gitignore — belt-and-suspenders in case it's ever committed anyway)
/docker-compose.yml
/docker-setup.sh
/.env.docker
/.env.docker.example

# Review-loop scratch state & stray build-output backup (see .gitignore —
# belt-and-suspenders in case either is ever committed anyway). js/*.mjs and
# img/*.png (the actual shipped assets) are unaffected: these patterns are
# anchored to the repo root only, not recursive.
/.review-tmp
/js.rootbak
/*.mjs
/*.png
